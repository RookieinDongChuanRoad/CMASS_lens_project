"""Reusable scientific model components.

The concrete model modules, such as `models.cmass`, should assemble components
from this package rather than hosting long formula implementations directly.
This keeps model selection readable and gives future models a consistent place
to reuse or replace individual scientific building blocks.
"""


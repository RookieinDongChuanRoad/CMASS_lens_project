Metadata-Version: 2.4
Name: cmass-posterior-predictive
Version: 0.1.0
Summary: Standalone posterior-predictive workflows for CMASS strong-lens analyses.
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: emcee>=3.1
Requires-Dist: h5py>=3.10
Requires-Dist: matplotlib>=3.8
Requires-Dist: numpy>=1.26
Requires-Dist: PyYAML>=6.0
Requires-Dist: cmass-lens-inference
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"

# 发现与决策

## 需求
- 对 `devauc` 和 `sersic` 的最新 MCMC 结果分别做 posterior predictive test。
- `theta_E` 和 `sigma` 使用同一总体生成逻辑、同一 strong-lens selection 权重。
- `theta_E` 每个 posterior draw 抽 23 个 replicated lenses。
- `sigma` 每个 posterior draw 再独立抽 7 个 replicated lenses。
- 每个 replicated lens 必须保留 latent 字段：`theta_E`、`gamma`、`z_d`、`z_s`、`profile`、`m5`、`R_e_kpc`；`sersic` 额外保留 `n`。
- 需要把 sigma 插值表需求写入 `prepare_intepolation_grids/2026-03-09-ppt-sigma-interpolation-requirements.md`。

## 调研发现
- `Posterior_predictive_test/` 当前为空目录，适合作为 planning 文件与结果工作区。
- `CMASS_lens_project` 当前不是 git 仓库。
- `Bayesian_inference` 已有标准 `emcee.backends.HDFBackend` 输出，`chain.h5` 可直接读取后验样本。
- 最新有效 run 路径：
  - `devauc`: `/Users/liurongfu/Work/CMASS_lens_project/outputs/devauc/latest -> 20260308_215643_devauc_devauc_prod_20260308`
  - `sersic`: `/Users/liurongfu/Work/CMASS_lens_project/outputs/sersic/latest -> 20260308_221211_sersic_sersic_prod_20260308`
- `devauc/20260308_215643_devauc_default` 是不完整空壳目录，不能当输入 run。
- normalization kernel 的 `1e5` 对应 `integration.normalization_samples`，它表示每次 `log_prob(theta)` 评估时使用的 Monte Carlo 样本数，不是 replicated sample lens 数。
- normalization kernel 复用固定 `base_normals`，因此同一 run 内的 normalization MC 基底是确定的。
- `prepare_intepolation_grids` 当前主实现是逐 galaxy 生成 `s2_grid`，但用户要求的是供 PPT 线程消费的大插值表。
- Jeans 插值表的最小推荐维度：
  - `devauc`: `(gamma, z_d, log10(R_e/kpc))`
  - `sersic`: `(gamma, z_d, log10(R_e/kpc), n)`
- 不应把 `m5/M5`、`theta_E`、`z_s`、`mstar` 作为 Jeans 插值轴。

## 技术决策
| 决策 | 理由 |
|------|------|
| 先写 PPC 测试再写实现 | 遵守 TDD，先锁定行为和接口 |
| PPC CLI 挂到现有 `cmass_lens_inference.cli` | 避免新增平行入口，便于后续直接用现有环境运行 |
| sigma 插值表值定义为 `S_unit = sigma^2 / 10**m5` | 这样质量归一化可在 PPT 端后乘恢复，减少插值维度 |
| PPC 结果目录采用 `<output_root>/<profile>/<run_id>/` | 与现有 inference 输出按 profile 分目录的习惯一致，便于批量管理 |
| `candidate_pool_size` 默认独立于 normalization sample count | normalization 的 `1e5` 是每次 `log_prob` 的 MC 估计规模，不适合直接拿来做每个 replicated draw 的显式候选池 |
| sigma 插值表消费格式先锁定为 `.npz` | 当前实现最简单、测试成本最低，也便于和对接线程约定明确字段名 |
| 监控入口新增固定文件名模式，而不是扫描整个目录 | 用户已明确外部线程可能直接覆盖 `jeans_deV_grid.h5` / `jeans_sers_grid.h5` |
| HDF5 sigma 表消费兼容两套 schema | 当前测试线程使用 PPC-native `*_axis` schema，但真实外部旧表仍是 `gamma_grid/z_grid/logRe_grid/(logn_grid)/s2_grid` |
| `sersic` legacy HDF5 表把 `logn_grid` 当作插值轴，查询时由 PPC 侧把 `n -> log10(n)` | 如果直接把 `10**logn` 当作新轴，会改变插值几何并产生系统性误差 |
| 负值容忍规则采用“负值占比不超过 5%，且最小值不低于 `-1e-4`” | 真实外部旧表存在约 `1e-6` 到 `1e-5` 量级的少量负值，属于可裁切的数值噪声 |
| PPC 默认 replicate 数量曾提升到 `100000`，但当前源码默认值已回退到 `1000` | `1e5` 只保留为一次历史性真实运行；例行命令恢复到更轻量的默认口径 |
| 保留全部 latent arrays 的前提下，单个 profile 的 `replicated_statistics.npz` 在 `1e5` 运行时约为 `171.7 MB` | 这是历史性 `1e5` 重跑留下的真实体积记录，便于后续评估是否再次启用大样本默认值 |
| canonical PPC 默认 posterior draw 口径改为尾部 `192000` draws | 用户最终确认 `devauc` 与 `sersic` 都按 `192000` 对齐；单 profile `devauc` 也不再保留更长尾部 |
| canonical PPC 默认 `candidate_pool_size` 改为 `100000` | 用户澄清真正要放大的参数是候选池，而不是 replicate 数量 |
| canonical PPC 主统计量改为 `median/std/p10/p90` | `mean` 已从 summary JSON、NPZ 和 overview 图的主统计量合同中移除 |
| canonical PPC 用 posterior-draw chunk 的 `process_pool` 并行 | 当前机器 `cpu_count=14`、`reserve_cores=2`，因此真实默认 worker 数是 `12` |

## 遇到的问题
| 问题 | 处理方式 |
|------|----------|
| 当前没有现成的大 sigma 插值表文件 | 设计成 CLI 显式接收表路径，并同步交付需求文档给对接线程 |
| 要保持与 normalization kernel 逻辑一致，但不能把 `1e5` 候选逐次全部展开 | 复用同一 latent 生成与 selection 权重公式，但把候选池大小单独做成 CLI / API 参数 |
| 另一线程先改了测试，但源代码仍只支持 `.npz` | 先跑红灯确认，再补 HDF5 读取和监控入口 |
| 真实外部目录中的旧 HDF5 表时间戳仍停留在 `2026-02-25 11:05:36 +08:00` | 视为 stale inputs，只做兼容性检查，不触发真实 PPT 运行 |

## 资源
- `/Users/liurongfu/Work/CMASS_lens_project/Bayesian_inference/src/cmass_lens_inference/cli.py`
- `/Users/liurongfu/Work/CMASS_lens_project/Bayesian_inference/src/cmass_lens_inference/model.py`
- `/Users/liurongfu/Work/CMASS_lens_project/Bayesian_inference/src/cmass_lens_inference/kernels/normalization.py`
- `/Users/liurongfu/Work/CMASS_lens_project/Bayesian_inference/src/cmass_lens_inference/posterior_predictive.py`
- `/Users/liurongfu/Work/CMASS_lens_project/Bayesian_inference/tests/conftest.py`
- `/Users/liurongfu/Work/CMASS_lens_project/prepare_intepolation_grids/interpolation_grids/physics/jeans.py`
- `/Users/liurongfu/Work/CMASS_lens_project/prepare_intepolation_grids/tests/test_jeans_regression.py`

## 外部表现状
- 当前真实外部文件：
  - `/Users/liurongfu/Work/CMASS_lens_project/data/external/jeans_deV_grid.h5`
  - `/Users/liurongfu/Work/CMASS_lens_project/data/external/jeans_sers_grid.h5`
- 二者当前 `mtime` 都早于监控门槛 `2026-03-09T15:27:07+08:00`，因此监控入口按设计不会启动 PPT。
- 在最新源码下，这两张旧表已经可以被 HDF5 loader 解析：
  - `jeans_deV_grid.h5 -> profile=devauc, shape=(17, 21, 21)`
  - `jeans_sers_grid.h5 -> profile=sersic, shape=(17, 21, 21, 21), uses_log10_n_axis=True`

## 最新真实运行
- 历史性的 `1e5` monitor 重跑后，两条 profile 的结果文件都记录为 `n_replicates = 100000`：
  - `devauc`: `run_manifest.json` / `ppc_summary.json` 均为 `100000`
  - `sersic`: `run_manifest.json` / `ppc_summary.json` 均为 `100000`
- 真实 `.npz` 文件大小：
  - `devauc`: `171.67 MB`
  - `sersic`: `171.67 MB`
- 当前源码默认值说明：
  - `posterior-predictive` CLI 默认 `n_replicates = 1000`
  - `posterior-predictive-monitor` CLI 默认 `n_replicates = 1000`
  - 两个 Python API 默认值也已同步回退到 `1000`

## 最新 canonical PPC 重跑
- 2026-03-10 的真实 canonical monitor 重跑已经完成，并覆盖了当前两个 profile 的结果目录。
- 最新 manifest 合同：
  - `devauc`: `n_posterior_draws_used = 192000`, `candidate_pool_size = 100000`, `posterior_draw_mode = tail_capped_full_chain`, `worker_processes = 12`
  - `sersic`: `n_posterior_draws_used = 192000`, `candidate_pool_size = 100000`, `posterior_draw_mode = tail_capped_full_chain`, `worker_processes = 12`
- 最新 summary 合同：
  - 两个 profile 的 `theta_ein` 和 `sigma` 统计键都变为 `median/p10/p90/std`
  - `mean` 不再作为 PPC 主统计量出现
- 真实完成时间可从结果文件 `mtime` 直接看到：
  - `devauc` `run_manifest.json`: `2026-03-10 21:29:42`
  - `sersic` `run_manifest.json`: `2026-03-10 21:39:11`

## Fig. 8 类趋势图实现发现
- histogram PPC 和 Fig. 8 趋势图是两类不同统计对象：
  - histogram PPC 比较的是固定样本量 replicated sample 的 summary statistics
  - Fig. 8 趋势图比较的是固定 `log M*` 条件下的总体均值曲线
- 因此趋势图不能直接重用 23-lens / 7-lens 的 replicated-sample 逻辑；必须单独实现“固定 mass-grid 点 -> 候选总体 -> 条件均值”的 evaluator。
- `Parent population` 的 `m5` 与 `gamma` 最适合直接用条件均值函数：
  - `mu5(m*) = mu5_0 + beta5 (m* - 11.4)`
  - `mu_gamma(m*) = mu_gamma_0 + beta_gamma (m* - 11.4)`
- `sigma_ap` 不能直接用均值函数，必须通过 sigma 插值表和无噪声物理模型计算。
- `Detectable lenses` 与 `SLACS-like selected` 的主要差别只在权重：
  - `w_det ∝ pi * (cs * theta_E)^2`
  - `w_sel ∝ P_find(theta_E) * pi * (cs * theta_E)^2`
- 真实外部 sigma 表足以支撑趋势图：
  - `devauc`: `/Users/liurongfu/Work/CMASS_lens_project/data/external/jeans_deV_grid.h5`
  - `sersic`: `/Users/liurongfu/Work/CMASS_lens_project/data/external/jeans_sers_grid.h5`
- 真实趋势图已产出：
  - `devauc`: `/Users/liurongfu/Work/CMASS_lens_project/Posterior_predictive_test/results/devauc/20260308_215643_devauc_devauc_prod_20260308/fig8_like.png`
  - `sersic`: `/Users/liurongfu/Work/CMASS_lens_project/Posterior_predictive_test/results/sersic/20260308_221211_sersic_sersic_prod_20260308/fig8_like.png`
- 当前正式运行口径：
  - `n_posterior_draws = 256`
  - `n_mass_grid = 40`
  - `logM* in [11.2, 12.1]`
  - `n_candidate_per_mass = 2000`

## 可视化/浏览器发现
- 论文 `Sonnenfeld 2024` 的 goodness-of-fit 章节使用 posterior predictive sample 与观测统计量比较的模式，核心统计量是分布的 `mean/std/p10/p90`。

## Notebook 对比专项发现
- `/Users/liurongfu/Desktop/Spectrum_reduction/inference.ipynb` 的 `## Inference with all 22 galaxies` 段落使用：
  - `chain = full_0103.h5`
  - `discard = 1000`
  - `num_parents = 10000`
  - `theta_E` sample size = `22`
  - `sigma` sample size = `7`
- notebook 的 sigma 表严格按以下方式构造：
- notebook 链最后两列的实际语义与当前 pipeline 顺序不一致：
  - notebook: `(loga, theta0)`
  - pipeline: `(theta0, loga)`
- notebook `compute_theta_ein_one()` 在旧 cell 里调用的是 gaussian `Pfind`，但用户已明确对比应采用修正版 baseline，即 sigmoid selection。
- notebook 的 `weights = g * pfind * norm` 中 `norm = P_SL_norm_mc(...)` 对同一 posterior draw 只是一个标量；在后续 `weights /= sum(weights)` 之后完全抵消，因此对 selected-lens 分布没有影响。
- notebook 原始实现混用了全局 `np.random` 与 joblib 并行，天然不利于复现实验；本地比较实现改为“每个 posterior sample index 一个确定性 seed”，可以更稳定地隔离实现差异。
- 真实 notebook 资产路径目前通过本机解析到：
  - chain: `/Users/liurongfu/datas/spectrums/chains/full_0103.h5`
  - sigma table: `/Users/liurongfu/datas/spectrums/jeans_sers_grid.h5`
  - cross section: `/Users/liurongfu/datas/spectrums/cs_grid_power.h5`
  - observations: `/Users/liurongfu/datas/spectrums/observations_with_m5_grids.hdf5`
- 这些路径与 `/Users/liurongfu/Desktop/Spectrum_reduction/data/...` 是同一批真实资产的当前解析位置；脚本仍保留桌面目录默认值，但运行结果会记录实际解析后的绝对路径。

## 新增实现
- 新增比较模块：
- 新增一次性运行脚本：
- 新增测试：

## 对比模块合同
- notebook baseline 与 pipeline-matched 两边都输出 `median/std/p10/p90`。
- 两边都使用同一条 post-burn-in chain；测试/调试时可用 `max_samples` 截断前若干 posterior samples。
- sigma 在这次对比中明确不加观测噪声。

## `full_0103.h5` 全量对比结果
- 全量作业已完成，正式结果目录：
  - `/Users/liurongfu/Work/CMASS_lens_project/Posterior_predictive_test/results/notebook_vs_pipeline/full_0103`
- 产物完整：
  - `comparison_summary.json`
  - `paired_differences.npz`
  - `comparison_overview.png`
  - `run_manifest.json`
- 合同核对通过：
  - `posterior_sample_count = 96000`
  - `sample_sizes = {theta_ein: 22, sigma: 7}`
- `theta_E` 的中心统计量非常接近：
  - `median` 的两边分布中位数相差约 `+0.009`
  - `p10` 的两边分布中位数相差约 `+0.003`
- `theta_E` 的高端与散度差异更明显：
  - `p90` 的两边分布中位数相差约 `+0.046`
  - `std` 的两边分布中位数相差约 `+0.050`
  - 但 `std` 的 paired difference 均值被极端样本显著拉高到 `+0.76`
- `sigma` 的四个统计量整体中心位置也很接近，但 paired differences 的离散度很大：
  - `median` 中位差约 `-0.46`
  - `std` 中位差约 `+0.003`
  - `p10` 中位差约 `-0.70`
  - `p90` 中位差约 `-0.72`
  - 其中 `sigma p90` 的 paired difference 尾部最宽，`1%-99%` 区间大约从 `-275.9` 到 `+270.5`

## 当前归因判断
- 这次对比已经显式控制了三类常见混淆项：
  - 参数顺序：已把 notebook `(loga, theta0)` 映射到 pipeline `(theta0, loga)`
  - sigma 后处理：已关闭额外观测噪声
- 因此，当前仍然保留下来的 notebook vs pipeline 差异，主因更可能来自：
  - 总体候选样本生成实现不同
  - selected distribution 的抽样细节不同
  - 两边 latent population 的随机基底构造不同
- 尤其因为 `theta_E` 统计量也仍有系统差异，而 `theta_E` 并不依赖 sigma 插值器，所以主要差异不能归因于 sigma 表读取。

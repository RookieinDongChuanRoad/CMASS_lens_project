# Workspace Scripts

Put user-facing orchestration and analysis scripts here.  Reusable library code
belongs under `src/statistical_sl/`; scripts in this directory should call the
package or CLI rather than owning scientific implementation details.
